spring:
  application:
    name: RLplatform
  datasource:
    url: jdbc:mysql://localhost:3306/rl_platform
    username: root
    password: qtq031113
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.springframework.web.servlet.mapping: DEBUG

#TODO: 需要修改为实际的路径
evaluation:
  python: "C:/Users/qtq/anaconda3/envs/mojito/python.exe"
  script: "scripts/evaluate.py"
  workspace: "E:/2025下/毕设/HalfCheetah-main" 

#logging:
#  level:
#    org:
#      springframework: DEBUG